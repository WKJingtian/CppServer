1. MySql太垃圾了，保证事务一致性的写法很麻烦，暂时就不加了，之后得换成PostgreSql。
2. 给每个用户开一个线程性能太差，应该IO多路复用，用select/epoll支持多个用户。
3. portable版本里的TTS没支持中文。
4. 输入和输出应该分开，不能被控制台输出打乱。
5. 扑克房间的状态输出不能太频繁（应该改动后才输出）。
6. 房间销毁逻辑：定时器销毁。
7. 加一个ping功能。